{
  "name": "angular-detect-changes-on-external-data",
  "version": "1.0.0",
  "description": "Angular — Detect changes on external Data",
  "scripts": {
    "postinstall": "cd ./angular_web_app && npm i && cd ../outer_emitter && npm i",
    "test": "echo \"Error: no test specified\" && exit 1",
    "angularWebApp": "cd ./angular_web_app && npm run start",
    "outerEmitter": "cd ./outer_emitter && npm run build:watch",
    "serve": "serve ./outer_emitter/dist/",
    "start": "npm-run-all --parallel angularWebApp serve"
  },
  "author": "J Krasemann",
  "license": "apache 2.0",
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "serve": "^14.2.0"
  }
}
